{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Director window: runs every command, listens to tournament changes and toggles its own full screen.",
  "windows": ["main"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "allow-list-tournaments",
    "allow-create-tournament",
    "allow-delete-tournament",
    "allow-get-view",
    "allow-dispatch",
    "allow-quote-deal",
    "allow-open-display-window",
    "allow-save-export",
    "allow-legacy-import-status",
    "allow-import-legacy",
    "core:window:allow-is-fullscreen",
    "core:window:allow-set-fullscreen"
  ]
}
